<Window x:Class="HotelManagementApp.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelManagementApp.View"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <Label Content="Email:" />
            <TextBox x:Name="EmailTextBox" Margin="0,0,0,10" />

            <Label Content="Password:" />
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20" />

            <Button x:Name="LoginButton" Content="Login" Click="LoginButton_Click" Height="30" />

            <TextBlock x:Name="ErrorMessageTextBlock" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>